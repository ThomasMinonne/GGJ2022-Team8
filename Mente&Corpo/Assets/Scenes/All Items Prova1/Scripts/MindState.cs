using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MindState : MonoBehaviour
{
	//Quando premi un pulsante non puoi più muoverti e cambi visuale
	//Jump Height = 1.2 
	//Move Speed = 4
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
		/*if(Input.GetKeyDown("space")){
			gameObject.GetComponent<FirstPersonController>().JumpHeight = 0;
			gameObject.GetComponent<FirstPersonController>().MoveSpeed = 0;
		}*/
    }
	
}

